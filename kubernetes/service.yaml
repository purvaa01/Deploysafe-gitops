apiVersion: v1
kind: Service
metadata:
  name: deploysafe-service
  namespace: deploysafe
  labels:
    app: deploysafe          # <-- Add this
spec:
  selector:
    app: deploysafe
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP